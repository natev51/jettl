Go back through and disregard any errors that don’t matter.
Like release references after an error..
who cares about the error from the release references..
there’s already an error from the obtain references method


Create Library
Teardown Library
Actor (Interface)
- Setup
- Handle
- Destroy
Queue Actor (Interface)
- Receive
- Last Ack
Event Actor (Interface)


Get rid of Ref, replace with Msg.

Create Actor (Interface)

So, Msg has input Rather of Actor in and Actor out.


replace Msg in Msg libraries with Msg Queue
This leaves the events to have the tag Msg Event for their libraries.